FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe
PIPER_PATH=piper

# Piper voice model. If PIPER_MODEL_PATH is empty, PIPER_VOICES_DIR/<voice>.onnx is used.
PIPER_MODEL_PATH=
PIPER_VOICES_DIR=

# Local LLM settings (llama-cpp-python preferred).
LLM_BACKEND=llama_cpp
LLM_MODEL_PATH=
LLM_HOOK_MODEL_PATH=
LLM_SCRIPT_MODEL_PATH=
LLM_MODEL_PATHS=
LLM_CTX=4096
LLM_MAX_TOKENS=512
LLM_TEMPERATURE=0.8
LLM_N_GPU_LAYERS=0

# Whisper/faster-whisper settings.
WHISPER_MODEL_SIZE=base
WHISPER_MODEL_PATH=
WHISPER_DEVICE=auto
WHISPER_COMPUTE_TYPE=auto

# Caption styling.
CAPTION_FONT=Impact
CAPTION_FONT_SIZE=72

# Optional paths (defaults are repo-relative if empty).
ASSETS_DIR=
BG_CLIPS_DIR=
FONTS_DIR=
OUTPUTS_DIR=
MODELS_DIR=
MUSIC_DIR=
SFX_DIR=
TEMPLATES_DIR=
CAPTION_STYLES_DIR=
PRESETS_PATH=
DB_PATH=
MAX_CONCURRENT_JOBS=2
SUBPROCESS_TIMEOUT_SECONDS=1800
ROUTING_MODE=manual
ROUTING_POLICY=balanced
WATCH_FOLDER_PATH=
WATCH_FOLDER_ENABLED=false
PLUGINS_ENABLED=
